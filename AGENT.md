# 자율 워크플로우 (Agent Mode)

이 문서는 main agent가 **사용자 개입 없이 단계를 자율적으로 진행**하기 위한 룰이다.
CLAUDE.md의 단계 정의를 보완하고, 각 단계마다 `reviewer` sub-agent를 호출해 검토받는 닫힌 루프를 명시한다.

---

## 트리거

다음 중 하나가 발생하면 이 워크플로우를 시작한다:
- `contract/` 폴더에 새 PDF가 들어왔고 사용자가 "ㄱㄱ" / "시작" / "go" 등 진행 신호를 줌
- 사용자가 명시적으로 "AGENT.md 따라 진행" 요청
- `/loop` 등으로 자율 모드가 켜진 경우

---

## 루프 구조

```
[1단계 산출]
   ↓
[reviewer 호출 — Task tool]
   ↓
   ├─ ✅ Pass + ⚠️만 → 권고 수정 후 다음 단계
   ├─ ❌ 블로커 있음 → 수정 → 같은 단계 재검토 (최대 2회)
   └─ 2회 재검토에도 블로커 → 사용자에게 보고 후 중단
   ↓
[2단계 산출]
   ↓
[reviewer 호출]
   ↓ ...
[3단계 산출 — HTML 14개 + 허브 + common.css]
   ↓
[reviewer 호출 — 3단계 전체 검토]
   ↓
[docs/index.html 누적 반영 — 시안 카드 그리드 갱신]
   ↓
[완료 보고]
```

---

## 각 단계 실행 절차

### 1단계: 기능 정의

1. 계약 PDF에서 개발 범위 발췌 (이미 확인했으면 스킵)
2. `01_FEAT.md` 작성 (CLAUDE.md 1단계 포맷)
3. **reviewer 호출**:
   ```
   Task(subagent_type="reviewer", prompt="1단계 (01_FEAT.md) 검토해줘. CLAUDE.md 기준으로 객관적으로.")
   ```
4. 결과 처리:
   - ✅ Pass + ⚠️ → 권고 항목 즉시 반영하고 다음 단계
   - ❌ 블로커 → 수정 후 재호출 (`재검토 부탁`)
   - 2회 실패 시 사용자에게 보고

### 2단계: 페이지 분리

1. 1단계 결과 기반으로 `02_PAGE.md` 작성 (표 + 트리)
2. **reviewer 호출** (2단계)
3. 결과 처리 (위와 동일)

### 3단계: HTML 시안

1. `pages/assets/css/common.css` 작성/갱신 (DESIGN.md 토큰 적용)
2. `pages/index.html` 허브 작성
3. 사용자 페이지 → 관리자 페이지 순서로 14개 HTML 작성
4. **reviewer 호출** (3단계 — 전체 디자인 톤·인터랙션·일관성 점검)
5. 결과 처리

### 마무리

1. `docs/index.html` 시안 섹션을 `pages/`의 14개 링크 카드로 갱신
2. 사용자에게 짧은 완료 보고:
   - 만든 파일 수
   - reviewer 통과 여부
   - 추가 조정 필요한 부분이 있는지

---

## 단계 간 사용자 확인 정책

기본은 **사용자 확인 없이 자율 진행**한다.
다만 다음 경우에만 사용자에게 묻는다:

- 계약서에서 개발 범위 해석이 두 가지 이상으로 갈리는 경우
- reviewer가 2회 연속 블로커 보고
- 계약서엔 없지만 추가해야 할 페이지가 5개 이상으로 늘어나는 경우 (스코프 확장)

그 외에는 **각 단계 자동 진행 + 완료 시 한 줄 보고**만.

---

## reviewer 호출 규칙

- **반드시 Task tool**로 호출 (subagent_type="reviewer")
- 프롬프트에 **단계 명시** + **검토 대상 파일 경로** 명시
- reviewer는 read-only이므로 그 결과를 main이 직접 반영
- reviewer 결과는 그대로 사용자에게 보여주지 말고, main이 **무엇을 수정했는지**만 요약 보고

---

## 자가 합리화 방지

- main은 자기가 만든 산출물을 절대 직접 검토하지 않는다 → 반드시 reviewer 호출
- reviewer가 "OK"라고 했다고 안주하지 말고, **⚠️ 권고도 가능한 반영**
- 같은 단계를 3회 이상 반복하면 사용자에게 보고 (무한 루프 방지)
